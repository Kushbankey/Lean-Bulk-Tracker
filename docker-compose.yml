services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - db-data:/app/server/prisma
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_URL=file:./dev.db
    restart: unless-stopped

volumes:
  db-data:
